services:
  mf-gestor-comercial-tienda-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mf-gestor-comercial-tienda-web
    ports:
      - "8083:80"
    networks:
      - gestor-network
    restart: unless-stopped

networks:
  gestor-network:
    external: true
